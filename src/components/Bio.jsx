import React from "react";

function Bio(){
  return (
    <div>
      <style jsx>{`
      p {
        text-align: center;
        border: solid 2px grey;
        margin: 10px 0 10px 0;
        padding: 30px;
      }
      }
    `}</style>
    <p>Before advancing, read the first two steps (Break the UI Into a Component Hierarchy and Build a Static Version in React) in the article that discuss how to mock-up a React application, break its UI into a component hierarchy, and build a static version. This is a really important skill to learn. Before advancing, read the first two steps (Break the UI Into a Component Hierarchy and Build a Static Version in React) in the article that discuss how to mock-up a React application, break its UI into a component hierarchy, and build a static version. This is a really important skill to learn.</p>
    </div>
  );
}

export default Bio;